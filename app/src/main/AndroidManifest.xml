<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bootcamp"
        tools:targetApi="31">
        <activity
            android:name=".database.UserCardsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.filter.FilterCardsActivity"
            android:exported="false"></activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Bootcamp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.authentication.AuthenticationButtonActivity"></activity>
        <activity android:name=".ui.authentication.AuthenticationActivity"></activity>
        <activity android:name=".ui.authentication.SignOutActivity"></activity>
    </application>

</manifest>
